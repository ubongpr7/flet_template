# name: Vercel Production Deployment

# env:
#   VERCEL_ORG_ID: ${{secret.VERCEL_ORG_ID}}
#   VERCEL_PROJECT_ID: ${{secret.VERCEL_PROJECT_ID}}
  
# on:
#   push:
#     branches:
#       - main

      
# jobs:
#   Deploy-Production:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Install Vercel CLI
#         run: npm install -global vercel

#       - name:  Pull vercel Environment Information
#         run: vercel pull --yes --environment=production --token= ${{secret.VERCEL_TOKEN}}
#       - name: Build Project
#         run: vercel build  --prod --token= ${{secret.VERCEL_TOKEN}}
#       - name: Deploy Project
#         run: vercel deploy --prebuilt  --prod --token= ${{secret.VERCEL_TOKEN}}
      

    
